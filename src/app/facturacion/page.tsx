import { Metadata } from "next/types";

export const metadata: Metadata = {
    title: 'Facturación',
};

export default function Billing(){
    return(
        <section>
            billing
        </section>
    );
}